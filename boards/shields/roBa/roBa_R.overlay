#include "roBa.dtsi"

&default_transform {
    col-offset = <6>;
};

/* 既存のtrackball定義を参照して修正します */
&trackball {
    /* 古いプロパティを削除 */
    /delete-property/ irq-gpios;
    /* 新しいプロパティを追加 */
    motion-gpios = <&xiao_d 2 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
};
